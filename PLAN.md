# پلن بازبینی و توسعه — اتوماسیون تغذیه صورتی

## ۱. حفظ UI کنونی
- تم صورتی + موبایل‌فراست + Bottom Nav حفظ بشه
- ولی از **shadcn/ui + Tailwind** استفاده بشه (نه MUI — سبک‌تر، ری‌اکتیوتر)
- طراحی RTL و فارسی حفظ بشه

## ۲. تغییر محل تحویل مجاز

**مفهوم:**
- هر کاربر بر اساس **گروه کاربری** (دانشجو/استاد/کارمند) → دسترسی به محل‌های تحویل مشخصی داره
- قیمت غذا **متفاوت** به ازای هر محل تحویل ممکنه باشه
- مثلاً: دانشجو سلف مرکزی = ۴۵,۰۰۰ / سلف خوابگاه = ۴۰,۰۰۰

**اجرا:**
- صفحه رزرو غذا: انتخابگر محل تحویل بالای لیست غذاها
- فیلتر غذاها بر اساس محل انتخاب شده
- قیمت پویا بر اساس محل + گروه کاربری

## ۳. کتابخانه UI
- **shadcn/ui** (built on Radix + Tailwind)
- حفظ تم صورتی از طریق CSS variables
- سازگاری کامل RTL

## ۴. پنل مدیریت — ماژول‌ها

### ۴.۱ مدیریت کاربران (`/panel/users`)
- **لیست کاربران** — جستجو، فیلتر بر اساس گروه/دانشکده
- **پروفایل کامل** — اطلاعات شخصی + مالی + رزروها
- **ویرایش کاربر** — نام، خانواده، کد ملی، گروه کاربری، دانشکده
- **تغییر/ریست رمز** — ریست رمز به مقدار پیش‌فرض + ارسال پیام
- **تخصیص گروه کاربری** — دانشجو/استاد/کارمند/مهمان → تعیین قیمت‌ها

### ۴.۲ گروه‌های دسترسی (`/panel/roles`)
- **مدیر اصلی** — دسترسی کامل
- **مدیر تغذیه** — مدیریت غذا و برنامه‌ریزی
- **مدیر تحویل** — مدیریت تحویل غذا
- **صندوقدار** — مشاهده گزارشات مالی
- **مشاهده‌گر** — فقط مشاهده

### ۴.۳ مدیریت تحویل (`/deliver`)
- **تحویل عادی** — اسکن کارت / وارد کردن کد ملی
- **تحویل اضافه** — تحویل غذا به کاربری که رزرو نکرده (ایجاد بدهکاری)
- **بدهکار کردن** — اضافه کردن مبلغ به حساب منفی کاربر
- **فروش غذای آزاد** — فروش غذاهای اضافه به هر کسی (بدون نیاز به رزرو)
- **وضعیت تحویل** — نمایش آمار لحظه‌ای (تحویل شده/در انتظار/لغو شده)

### ۴.۴ سیستم گزارش‌ساز (`/panel/reports`)
**کامپوننت مشترک `ReportBuilder`:**
- **فیلترها:**
  - بازه زمانی (تاریخ شروع/پایان، امروز، فردا، این هفته، هفته خاص)
  - گروه کاربری (دانشجو/استاد/کارمند/همه)
  - دانشکده
  - نوع غذا
  - محل تحویل
  - وضعیت (رزرو شده/تحویل شده/لغو شده)
  - مبلغ (بازه قیمت)
- **ستون‌های قابل انتخاب** — کاربر می‌تونه ستون‌ها رو اضافه/حذف کنه
- **خروجی:** جدول + نمودار + خروجی اکسل
- **ذخیره قالب** — ذخیره فیلترها به عنوان قالب گزارش

**انواع گزارش:**
- گزارش مالی (درآمد، هزینه، مانده حساب)
- گزارش تحویل (تعداد تحویل‌ها به تفکیک روز/هفته)
- گزارش رزرو (رزروهای فعال، لغو‌شده، انجام شده)
- گزارش کاربران (پرمصرف، کم‌مصرف، بدهکار)
- گزارش غذاها (محبوب‌ترین، کم‌فروش‌ترین)

## ۵. تقویم فارسی
- **moment-jalaali** یا **date-fns-jalali** برای تمام تاریخ‌ها
- **date picker فارسی** در تمام فیلترها و فرم‌ها
- نمایش تاریخ‌ها به صورت `۱۴۰۳/۰۶/۱۳` در تمام جاها

## ۶. سیستم ACL (Access Control List)

### ساختار:
```
User → Role (مدیر اصلی، مدیر تغذیه، ...) → Permissions
Permission = { resource: 'reports', action: 'view', scope: 'own-faculty' }
```

**منابع قابل کنترل:**
- صفحات (مشاهده/ویرایش)
- گزارشات (انواع مختلف)
- عملیات (رزرو/تحویل/بدهکاری/فروش)
- داده‌ها (فقط دانشکده خود / همه دانشکده‌ها)

### اجرا:
- **فرانت:** دکمه‌ها/صفحات غیرمجاز مخفی یا غیرفعال
- **بک‌اند:** middleware بررسی permission قبل از هر API call

## ۷. ترتیب اجرا (پیشنهادی)

| مرحله | ماژول | زمان تقریبی |
|-------|-------|-------------|
| ۱ | shadcn/ui + تم صورتی + تقویم فارسی | پایه |
| ۲ | تغییر محل تحویل + قیمت پویا | کوتاه |
| ۳ | مدیریت کاربران (لیست/ویرایش/ریست رمز) | متوسط |
| ۴ | گروه‌های دسترسی (ACL) | متوسط |
| ۵ | مدیریت تحویل (عادی/اضافه/بدهکاری/آزاد) | بلند |
| ۶ | سیستم گزارش‌ساز | بلند |
| ۷ | تست و نهایی‌سازی | متوسط |

---

**سوالات قبل از شروع:**
1. آیا shadcn/ui اوکیه یا MUI ترجیح میدید؟
2. آیا ترتیب اجرا اوکیه یا اولویت‌ها فرق داره؟
3. آیا بک‌اند API هم باید ساخته بشه یا فقط فرانت با demo data؟
